# Copy this file to config/secrets.toml and update the credentials.
# The secrets file is never committed and overrides values from default.toml.

# Optional API overrides; delete keys you do not need.
[api]
baseUrl = "http://localhost:5380/api"
timeoutMs = 15000
verifyTls = true

[auth]
username = "admin"
password = "change-me"
token = ""
totp = ""
